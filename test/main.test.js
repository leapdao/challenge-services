// @format
const { assert } = require("chai");

const main = require("../src/main");

describe("Event Scanner", () => {
  it("should set a block height in the database if none is defined", () => {});
});
